import os
import streamlit as st
from openai import OpenAI
from docx import Document
from io import BytesIO
import fitz  # PyMuPDF

# --- НАСТРОЙКА ---
st.set_page_config(page_title="Аудитор 🧛", layout="wide", page_icon="🧛")

def extract_text(file):
    try:
        if file.name.endswith('.pdf'):
            doc = fitz.open(stream=file.read(), filetype="pdf")
            return "".join([f"\n[СТРАНИЦА {i+1}]\n{p.get_text()}" for i, p in enumerate(doc)])
        else:
            doc = Document(file)
            return "".join([f"[Абзац {i+1}] {p.text}\n" for i, p in enumerate(doc.paragraphs) if p.text.strip()])
    except Exception as e:
        st.error(f"Ошибка чтения файла: {e}")
        return ""

def load_bad_history():
    if os.path.exists("bad_history.txt"):
        try:
            with open("bad_history.txt", "r", encoding="utf-8") as f:
                return f.read()
        except:
            return "База прошлых отказов пуста."
    return "База прошлых отказов пуста."

def create_docx(text):
    doc = Document()
    doc.add_heading('ПРОТОКОЛ НЕСООТВЕТСТВИЙ', 0)
    for line in text.split('\n'):
        doc.add_paragraph(line)
    buf = BytesIO()
    doc.save(buf)
    buf.seek(0)
    return buf

# --- ИНТЕРФЕЙС ---
with st.sidebar:
    st.header("⚙️ Параметры AI")
    st.success("Используется модель: **DeepSeek-V3**")
    st.caption("Версия V3 обеспечивает максимальную точность юридического аудита.")

st.title("🧛 Симулятор Вредного Заказчика")
st.markdown("### Глубокий аудит ваших документов")

col1, col2 = st.columns(2)
with col1:
    contract_file = st.file_uploader("📄 КОНТРАКТ", type=['pdf', 'docx'])
with col2:
    report_file = st.file_uploader("📝 ОТЧЕТ", type=['pdf', 'docx'])

if st.button("🚀 ЗАПУСТИТЬ ТОТАЛЬНЫЙ АУДИТ"):
    if contract_file and report_file:
        try:
            api_key = st.secrets.get("DEEPSEEK_API_KEY")
            client = OpenAI(base_url="https://api.deepseek.com", api_key=api_key)
            
            with st.status("🔍 Запуск аудита DeepSeek-V3...", expanded=True) as status:
                c_text = extract_text(contract_file)
                r_text = extract_text(report_file)
                bad_history = load_bad_history()
                
                if not c_text or not r_text:
                    st.error("❌ Файлы не прочитаны.")
                    st.stop()
                
                status.update(label="🧠 ИИ изучает документы...", state="running")

                # --- ВОЗВРАЩАЕМ ТОТ САМЫЙ МОЩНЫЙ ПРОМПТ ---
                sys_msg = f"""Ты — максимально придирчивый аудитор госконтрактов. Твоя цель: найти повод НЕ ПРИНИМАТЬ отчет.
                
                ИСПОЛЬЗУЙ 3 СЛОЯ ПРОВЕРКИ:
                1. ИСТОРИЧЕСКИЙ СЛОЙ (База прошлых отказов):
                {bad_history}
                
                2. ТЕХНИЧЕСКИЙ СЛОЙ (ТЗ):
                Сверь каждый факт, цифру, дату и объем. Малейшее отклонение — нарушение.
                Проверь ИНК, полные названия ведомств, отсутствие стоп-слов ("около", "порядка", "не менее").

                3. ЛИНГВИСТИЧЕСКИЙ СЛОЙ (Тотальный контроль. Стиль и формализм):
                Ищи и фиксируй следующие нарушения:
                - **Слова-паразиты и неопределенность**: "около", "примерно", "порядка", "в районе", "не менее" (если в ТЗ указано точное число), "и т.д.", "и др.".
                - **Нарушение официально-делового стиля**: использование разговорных оборотов, сокращений (кроме общепринятых), отсутствие терминологического единства (в Контракте один термин, в Отчете — другой).
                - **Топонимы и субъекты**: использование "РФ" вместо "Российская Федерация", сокращение названий министерств и ведомств без предварительной расшифровки.
                - **Орфографические и пунктуационные ошибки**: влияющие на смысл юридических условий.
                - **Модальность**: замена "Обязан" на "Может" или "Рекомендуется".
                - **Реквизиты**: ошибки в ИНК, КПП, номерах счетов, датах и ФИО.

                ДЛЯ КАЖДОЙ ОШИБКИ ПИШИ СТРОГО:
                - **Нарушение**: (конкретная суть)
                - **Локация**: (Страница №, Абзац №)
                - **Основание**: (пункт из ТЗ или пункт из Базы отказов)
                - **Риск**: (почему это приведет к штрафу или возврату)
                - **КАК ИСПРАВИТЬ**: (Напиши здесь конкретную формулировку или действие, чтобы снять замечание. Для лингвистических ошибок — дай правильный вариант предложения).
                """

                usr_msg = f"""Проведи аудит. Используй маркеры [СТРАНИЦА] и [Абзац].
                
                КОНТРАКТ:
                {c_text[:12000]}
                
                ОТЧЕТ:
                {r_text[:12000]}"""

                # ПРЯМОЙ ВЫЗОВ БЕЗ ПЕРЕМЕННЫХ-ФАНТОМОВ
                res = client.chat.completions.create(
                    model="deepseek-chat",
                    messages=[
                        {"role": "system", "content": sys_msg},
                        {"role": "user", "content": usr_msg}
                    ],
                    temperature=0.1,
                    max_tokens=4000
                )
                
                report_content = res.choices[0].message.content
                status.update(label="✅ Аудит завершен!", state="complete", expanded=False)

            # --- ВЫВОД ---
            if report_content:
                st.subheader("📋 Итоговый протокол с рекомендациями")
                st.markdown(report_content)
                st.download_button("📥 Скачать протокол", data=create_docx(report_content), file_name="Audit_Report.docx")

        except Exception as e:
            st.error(f"❌ Ошибка: {str(e)}")
    else:
        st.warning("⚠️ Загрузите документы.")
